# Firebase
.firebase/
*-debug.log
.runtimeconfig.json
.env.local

# Dependencies
node_modules/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Transient task-runner / agent state
.juno_task/
.env
.claude/
.cursor/

# Large, non-deterministic per-run capture dump (the HTTP snapshot + summary.json
# already record the evidence; the rendered-DOM blob just churns history).
reports/browser-evidence/production-rendered-dom.html
