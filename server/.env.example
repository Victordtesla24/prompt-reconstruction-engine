# Prompt Reconstruction Engine — backend environment
# Copy to .env on the VPS (chmod 600). NEVER commit the real key.
OPENROUTER_API_KEY=sk-or-v1-...
RECON_PORT=8791
RECON_RATE_LIMIT=20
RECON_DAILY_CAP=500
